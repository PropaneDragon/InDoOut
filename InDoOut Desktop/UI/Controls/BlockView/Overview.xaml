<UserControl x:Class="InDoOut_Desktop.UI.Controls.BlockView.Overview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InDoOut_Desktop.UI.Controls.BlockView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" PreviewMouseDoubleClick="UserControl_PreviewMouseDoubleClick">
    <Grid>
        <Border SnapsToDevicePixels="True" BorderThickness="1" CornerRadius="0" Background="#E5161616">
            <Grid>
                <Canvas x:Name="Canvas_Layout">

                </Canvas>
                <Canvas x:Name="Canvas_View">
                    <Rectangle x:Name="Rectangle_Viewport" SnapsToDevicePixels="True" Width="100" Height="100" Stroke="#99FFFFFF" />
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</UserControl>
